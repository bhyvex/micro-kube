# Official CoreOS channel from which updates should be downloaded.

# $update_channel='alpha'

# Change the version of CoreOS to be installed.  To deploy a specific version,
# simply set $image_version accordingly.  For example, to deploy version
# 709.0.0, set $image_version="709.0.0".  The default value is "current", which
# points to the current version of the selected channel.

# $image_version = "current"

# Customize VMs:

# $vm_memory = 1024
# $vm_cpus = 1
# $ip = '172.17.8.100'
